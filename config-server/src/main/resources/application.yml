server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MalikBandara/config-server.git
          default-label: main
          clone-on-start: true


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka